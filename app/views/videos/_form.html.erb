<%= simple_form_for(video) do |f| %>
  <% if video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(video.errors.count, "error") %> prohibited this video from being saved:</h2>

      <ul>
      <% video.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.input :title %>
  </div>

  <div class="form-group">
    <%= f.input :tag_list %>
  </div>

  <div class="form-group">
    <%= f.input :url %>
  </div>

  <div class="form-group">
    <%= f.input :description %>
  </div>

  <div class="form-group">
      <%= button_tag type: 'submit', id: "new-video",class: "btn btn-primary"  do %>
        <i class="fa fa-btn fa-youtube-play"></i> Upload Video
      <% end %>
  </div>
<% end %>
