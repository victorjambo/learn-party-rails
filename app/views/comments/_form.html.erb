<%= simple_form_for([@video, @comment]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :body %>
    <%= f.association :video %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
